apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml
  - hpa.yaml

commonLabels:
  app: tts-gateway

# Apply with:
#   oc apply -k deploy/openshift
# After creating the secret out-of-band:
#   oc create secret generic tts-gateway-secrets --from-literal=admin-api-key="$(openssl rand -hex 32)"
